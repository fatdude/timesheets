<% if @activity.errors.empty? -%>
  $.facebox.close();
  $.jGrowl('<%= growl %>');
  $('table#activities tbody').prepend('<%= escape_javascript(render @activity) %>');
  $('tr[data-id=<%= @activity.id %>] a[data-display=popup]').facebox();
  highlight_alt_rows('table#activities');
<% else -%>
  $('form.new_activity').replaceWith('<%= escape_javascript(render 'form') %>');
<% end -%>

