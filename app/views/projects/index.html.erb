<% if @client -%>
  <h2><%= @client.name %> projects</h2>
<% else -%>
  <h2>All projects</h2>
<% end -%>

<table id="projects">
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <%= render @projects %>
  </tbody>
</table>

<div class="controls">
  <% if @client -%>
    <%= link_to 'New activity', new_client_project_path(@client), :class => 'bigpic add', 'data-display' => 'popup' %>
  <% else -%>
    <%= link_to 'New activity', new_project_path, :class => 'bigpic add', 'data-display' => 'popup' %>
  <% end -%>
</div>

